---
layout: base
---

{% include "navigation-sticky.njk" %}

<div class="container text-center py-5">
  <img src="{{ image }}" alt="{{ title }}" class="rounded-circle" width="150">
  <h1>{{ title }}</h1>
  <h3 class="text-muted">{{ role }}</h3>
  <p class="mt-3">{{ bio }}</p>
</div>

{% include "footer.njk" %}
