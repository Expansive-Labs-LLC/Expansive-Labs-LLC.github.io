<!-- src/layouts/post.njk -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>

    <!-- Include Soft UI Styles -->
    <link rel="stylesheet" href="/static/assets/css/main.css">
</head>
<body>
    <!-- Navbar (Include your theme’s navbar if applicable) -->
    {% include "navigation-sticky.njk" %}

    <div class="container mt-5">
        <article class="card shadow-lg p-4">
            {% if image %}
            <img src="{{ image }}" alt="{{ title }}" class="img-fluid rounded">
            {% endif %}

            <h1 class="mt-3">{{ title }}</h1>
            <p class="text-muted"><strong>{{ author }}</strong> - {{ date }}</p>

            <div class="content">
                {{ content | safe }}
            </div>
        </article>

        <!-- Back to Blog Button -->
        <div class="mt-4">
            <a href="/blog/" class="btn btn-primary">Back to Blogssss</a>
        </div>
    </div>

    <!-- Footer -->
    {% include "footer.njk" %}
</body>
</html>
